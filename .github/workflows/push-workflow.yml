name: Push Trigger Workflow

on:
  push:
    branches:
      - main

jobs:
  push-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print success message
        run: echo "Code was successfully pushed to the main branch!"

      - name: Display date and time
        run: date

      - name: Display current working directory
        run: pwd
